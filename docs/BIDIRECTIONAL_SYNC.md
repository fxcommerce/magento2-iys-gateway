# Bidirectional synchronization

The connector publishes independent timestamps for EMAIL, SMS and CALL and applies newer IYS decisions through signed pull/ACK endpoints without creating outbound echo events.
